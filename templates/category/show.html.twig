{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block main %}
	<main class="content">

    <h2>Categorie</h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_index') }}">Retourner à la liste</a>

    <a href="{{ path('app_category_edit', {'id': category.id}) }}">Editer</a>

    {{ include('category/_delete_form.html.twig') }}
    <main>
{% endblock %}
