<form method="post" action="{{ path('app_articles_delete', {'id': article.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer les modifications?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.id) }}">
    <button class="article--admin" class="btn">Supprimer</button>
</form>
